<div class="topic">
	<div class="container">
		<div class="row">
			<div class="col-sm-6">
				<h3><%= yield(:title) %></h3>
			</div>
			<div class="col-sm-6">
				<% if content_for? :breadcrumb %>
				<%= yield :breadcrumb %>
				<% end %>
			</div>
		</div>
	</div>
</div>